# Demo Credentials (Template)

Replace placeholders with your deployed environment credentials before submission:

- Super Admin: `admin@chaintrace.demo` / `<password>`
- Producer: `producer@chaintrace.demo` / `<password>`
- Distributor: `distributor@chaintrace.demo` / `<password>`
- Inspector: `inspector@chaintrace.demo` / `<password>`

Public verification does not require login.

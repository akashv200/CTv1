// MongoDB model removed. Users are now stored in PostgreSQL table `users`.
export type UserRole = "super_admin" | "org_admin" | "producer" | "distributor" | "retailer" | "inspector" | "consumer" | "auditor";
